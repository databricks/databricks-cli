sudo: true
language: python
matrix:
  include:
    - python: 2.7
      env: TOXENV=py27
    - python: 3.6
      env: TOXENV=py36
    - python: 3.7
      env: TOXENV=py37

install:
  - pip install --upgrade pip
  - pip install .
  - pip install -r dev-requirements.txt
script:
  - pip list
  - tox
