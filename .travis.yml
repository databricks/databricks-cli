sudo: true
language: python
python:
  - 2.7
  - 3.4
  - 3.5
  - 3.6
install:
  - pip install --upgrade pip
  - pip install -r dev-requirements.txt
script:
  - pip list
  - ./lint.sh
  - pip install .
  - pytest tests
