sudo: true
language: python
matrix:
  include:
    - python: 2.7
      env: TOXENV=py27
install:
  - pip install --upgrade pip
  - pip install .
  - pip install -r dev-requirements.txt
script:
  - pip list
  - tox
